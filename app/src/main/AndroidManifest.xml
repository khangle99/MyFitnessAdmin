<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khangle.myfitnessadmin">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:name=".MyFitnessApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFitnessAdmin">
        <activity android:name=".nutrition.detail.MenuDetailActivity" />
        <activity android:name=".nutrition.menuList.MenuListActivity" />
        <activity android:name=".nutrition.category.NutritionCategoryActiviy" android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />
        <activity android:name=".excercise.excdetail.ExcerciseDetailActivity" />
        <activity android:name=".excercise.category.ExcerciseCategoryActivity" android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />
        <activity android:name=".excercise.exclist.ExcerciseListActivity" />
        <activity
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:name=".MainActivity"
            android:label="Fitness Admin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>